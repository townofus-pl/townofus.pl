import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Role"
};

export default function RoleLayout({ children }: { children: React.ReactNode }) {
    return children;
}
