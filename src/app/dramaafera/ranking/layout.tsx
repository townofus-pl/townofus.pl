import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Ranking"
};

export default function RankingLayout({ children }: { children: React.ReactNode }) {
    return children;
}
