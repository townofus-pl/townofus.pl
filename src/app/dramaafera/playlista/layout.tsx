import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Playlista"
};

export default function PlaylistaLayout({ children }: { children: React.ReactNode }) {
    return children;
}
