import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Historia Gier"
};

export default function HistoriaGierLayout({ children }: { children: React.ReactNode }) {
    return children;
}
