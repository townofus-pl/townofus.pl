import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Informacje"
};

export default function InformacjeLayout({ children }: { children: React.ReactNode }) {
    return children;
}
