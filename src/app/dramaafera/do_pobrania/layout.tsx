import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Do Pobrania"
};

export default function DoPobraniaLayout({ children }: { children: React.ReactNode }) {
    return children;
}
