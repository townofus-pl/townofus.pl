import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Changelog"
};

export default function ChangelogLayout({ children }: { children: React.ReactNode }) {
    return children;
}
