import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Drama Afera - Changelog - Mira"
};

export default function MiraChangelogLayout({ children }: { children: React.ReactNode }) {
    return children;
}